#include "main.h"

/**
 * isupper - checks if a letter is supper
 * @x: the number to be checked
 * Return: 1 for upper case or 0 for any else
 */
int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
